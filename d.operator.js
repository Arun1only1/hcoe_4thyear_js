// ? operator => symbol which has specific action

// ? arithmetic operator
// ? assignment operator
// ? comparison operator
// ? logical operator

// ? arithmetic operator
// ? + ,- , *, /, **, %

// ? assignment operator
// ?=, +=, -=, *=, /=, **=, %=

// let x = 2;

// x += 5; //  x= x+5

// console.log(x);

// let y = 7;

// y %= 2; // y = y % 2

// console.log(y);

// ? comparison operator => always results in boolean(true/false)
// ? ==,===, !=, !==, < , >, <= ,>=

// ? == => checks value equality / weak comparison
// console.log("2" == 2);

// ? === => checks value equality + checks data type equality / strict comparison
// console.log("2" === 2);

// console.log("Nepal" == "nepal");

// console.log("25" === 25);

// ? != => checks value only

// console.log(2 != "2");

// ? !== checks value + data type
// console.log("HCOE" !== "hcoe");

// console.log(2 <= 3);

// console.log(4 >= 4);

// ? logical operator
// ? AND (&&) => * gate
// ? OR (||) => + gate
// ? NOT (!)

// console.log(true && true && false && true); //false

// console.log(false || false || true || false); // true

// console.log(!false);
