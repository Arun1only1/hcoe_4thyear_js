// ?data types

// ? dynamic language
// ? uses interpreter

// ? string
// ? number
// ? boolean
// ? null
// ? undefined
// ? object
// ? bigInt
// ? symbol

// let x = 8 / 0;
// let x = "hcoe" / 2;
// ? NaN => Not a Number
// console.log(x, typeof x);

// ? string=> "",'',``
// let x = `Nepal`;
// let x = "25";
// console.log(x, typeof x);

// ? boolean
// let x = false;
// console.log(x, typeof x);

// ? undefined
// let x;

// console.log(x, typeof x);

// ? null

// let x = null;
// console.log(x, typeof x);
// ? here typeof x is object which is historic bug

// ? object
// ? key value pair => object
// const userDetail = {
//   name: "rohan@107",
//   address: "ktm",
// };

// console.log(userDetail, typeof userDetail);

// const numList = [1, 2, 3];
// console.log(numList, typeof numList);
